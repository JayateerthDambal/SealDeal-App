rules_version = '2';

// Allow read and write access to all users for local development.
// We will make this much more secure before going to production.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}